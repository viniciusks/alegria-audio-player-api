import { Router, Request, Response } from "express";
import app from "../services/firebase.service";

const usersRoutes = Router();

usersRoutes.post("/login", (request: Request, response: Response) => {
  app;
});
